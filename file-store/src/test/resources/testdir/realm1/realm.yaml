# yaml-language-server: $schema=../../schema/v1/file/schema-realm.yaml

version: 1

name: name
attributes:
  a: '11'
  b: 
    - aa
    - bb

components:
  c1: 
    providerId: xyz
    aa: aa
    bb: bb

realmKeys: ## i.e. components/*/providerType = org.keycloak.keys.KeyProvider
  sig:  # components/*/config[use=sig]
    providerId: rsa
    private-key: AAA...ZZZ
    public-key: AAA...ZZZ
    certificate: AAA...ZZZ
  enc:
    providerId: rsa
    private-key: AAA...ZZZ
    public-key: AAA...ZZZ
    certificate: AAA...ZZZ


clients: $glob:clients/*.yaml

groups:
  admin:
  user:

users:
  user1:
    groups: [ admin, user ]

